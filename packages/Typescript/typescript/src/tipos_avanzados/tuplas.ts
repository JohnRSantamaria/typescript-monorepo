//Tuplas
export const userID: (number | string)[] = [1, 'uno'];
userID.push(2);
userID.push('dos');
userID.push('dos');

//Son arrays de JAVA ::coffe::
export const user_id: [string, number] = ['uno', 1];

export const [db_nosql, db_sql] = user_id;

console.log(user_id);
